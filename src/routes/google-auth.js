// Google OAuth olib tashlandi.
// Developer panel endi login/parol bilan kiradi (qarang: src/routes/developer.js -> POST /login).
const express = require('express');
const router = express.Router();
module.exports = router;
